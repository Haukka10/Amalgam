using UnityEngine;
using Unity.VisualScripting;

public enum CardDomain { K, P, M, D } // Doamin Card
public enum CardType { Normal, Modifier } // Type card
public enum SlotType { BACK, MOD, MID, FRONT, BATTLEFIELD, Valhalla, Graveyard } //Slot Type
public enum Player { Human, AI }

namespace CardGame.Structures
{
    public class Structures
    {

    }
}
