using UnityEngine;
using Unity.VisualScripting;

public enum CardDomain { K, P, M, D } // Domeny kart
public enum CardType { Normal, Modifier } // Typ karty
public enum SlotType { BACK, MOD, MID, FRONT, BATTLEFIELD, Valhalla, Graveyard }
public enum Player { Human, AI }

namespace CardGame.Structures
{
    public class Structures
    {

    }
}
